# 服务器端口配置
PORT=3000

# 可选：默认API密钥（如果请求中没有提供API key时使用）
# DEFAULT_API_KEY=your-default-api-key-here

# 可选：强制使用默认API密钥，忽略请求头中的authHeader（默认: false）
# 当设置为 true 时，将完全忽略请求头中的 authorization 和 x-api-key，直接使用 DEFAULT_API_KEY
# FORCE_DEFAULT_API_KEY=true

# 可选：默认用户ID（用于metadata字段）
# 实际使用时会格式化为：user_{DEFAULT_USER_ID}_account__session_{uuid}
# 例如：user_myuser_account__session_0f0e7d1b-8dcc-4251-ae73-fc301079bac1
# DEFAULT_USER_ID=your-custom-user-id

# 可选：日志级别 (debug, info)
LOG_LEVEL=info

# 可选：目标API URL（如果需要自定义）
# TARGET_API_URL=https://api.packycode.com/v1/messages?beta=true
